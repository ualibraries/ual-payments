apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ua-payments-charts.fullname" . }}-test-connection"
  labels:
    {{- include "ua-payments-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ua-payments-charts.fullname" . }}-app:{{ .Values.app.service.port }}']
  restartPolicy: Never
