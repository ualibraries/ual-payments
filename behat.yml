imports:
  # settings specific to a particular machine
  - ./behat.local.yml
default:
  extensions:
    Behat\MinkExtension:
      sessions:
        default:
          goutte: ~
        javascript:
          selenium2: ~
