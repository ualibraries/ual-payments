imports:
  # settings specific to a particular machine
  - ./behat.local.yml
default:
  extensions:
    Behat\MinkExtension:
      sessions:
        default:
          goutte: ~
        javascript:
          browser_stack:
            capabilities:
              browser: 'firefox'
              browserstack-tunnel: true
